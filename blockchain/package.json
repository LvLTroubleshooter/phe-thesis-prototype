{
  "name": "phe-thesis-prototype-blockchain",
  "version": "0.1.0",
  "private": true,
  "description": "Reusable local private blockchain foundation for dissertation audit experiments.",
  "scripts": {
    "node": "hardhat node",
    "deploy:localhost": "hardhat run scripts/deploy.js --network localhost",
    "test": "hardhat test"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.19"
  }
}
